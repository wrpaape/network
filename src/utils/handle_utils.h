#ifndef NETWORK_UTILS_HANDLE_UTILS_H_
#define NETWORK_UTILS_HANDLE_UTILS_H_

/* external dependencies
 * ─────────────────────────────────────────────────────────────────────────── */
#include "utils/fail_switch.h"	/* bool, errno, FAIL_SWITCH, error macros */
#include "utils/closure.h"	/* Handler, HandlerClosure */

#endif /* ifndef NETWORK_UTILS_HANDLE_UTILS_H_ */
